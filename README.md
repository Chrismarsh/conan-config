Configuration files for conan for building CHM from source.

This is predominately used for the CI builds of CHM dependencies.

Changes include:

```    Linux:
        distro: [None, "ubuntu-18.04", "ubuntu-20.04"]
```